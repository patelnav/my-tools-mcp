{
  "name": "test-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'build'",
    "test": "echo 'test'",
    "start": "echo 'start'",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "db:help": "drizzle-kit --help",
    "db:generate:help": "drizzle-kit generate --help",
    "db:push:help": "drizzle-kit push --help",
    "db:studio:help": "drizzle-kit studio --help",
    "db:check:help": "drizzle-kit check --help",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:check": "drizzle-kit check"
  },
  "dependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "drizzle-kit": "^0.20.0",
    "drizzle-orm": "^0.29.0",
    "tsup": "^8.0.0",
    "eslint": "^8.0.0"
  }
} 